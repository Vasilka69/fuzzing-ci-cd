apiVersion: v1
kind: ServiceAccount
metadata:
  name: build-service
  labels:
    app.kubernetes.io/name: build-service
    app.kubernetes.io/component: executor
    app.kubernetes.io/part-of: fuzzing-ci-cd
automountServiceAccountToken: false
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: build-service-config
  labels:
    app.kubernetes.io/name: build-service
    app.kubernetes.io/component: executor
    app.kubernetes.io/part-of: fuzzing-ci-cd
data:
  SERVICE_NAME: "build-service"
  SERVER_PORT: "8080"
  KAFKA_BOOTSTRAP_SERVERS: "kafka:9092"
  KAFKA_CONSUMER_GROUP: "build-service-executor"
  KAFKA_CLIENT_ID: "build-service"
  CICD_EXECUTOR_TOPIC: "jobs.build"
  CICD_WORKER_ID: "build-worker-1"
  WORKER_ID: "build-worker-1"
  LOGGING_STRUCTURED_FORMAT_CONSOLE: "logstash"
  EXECUTOR_EVENTS_TRANSPORT: "kafka"
  OPENSEARCH_LOGS_ENABLED: "true"
  OPENSEARCH_ENDPOINT: "http://opensearch:9200"
  OPENSEARCH_EVENTS_INDEX: "executor-events"
  STORAGE_ENDPOINT: "http://storage-service:8080"
  WORKSPACE_ROOT: "/workspace"
  JAVA_TOOL_OPTIONS: "-Djava.io.tmpdir=/tmp -Duser.home=/tmp"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: build-service
  labels:
    app.kubernetes.io/name: build-service
    app.kubernetes.io/component: executor
    app.kubernetes.io/part-of: fuzzing-ci-cd
    app.kubernetes.io/version: "0.1.0-SNAPSHOT"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: build-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: build-service
        app.kubernetes.io/component: executor
        app.kubernetes.io/part-of: fuzzing-ci-cd
    spec:
      serviceAccountName: build-service
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: build-service
          image: fuzzing-ci-cd/build-service:0.1.0-SNAPSHOT
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: build-service-config
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
              ephemeral-storage: "2Gi"
            limits:
              cpu: "2"
              memory: "2Gi"
              ephemeral-storage: "8Gi"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: workspace
              mountPath: /workspace
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 18
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
      volumes:
        - name: tmp
          emptyDir: {}
        - name: workspace
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: build-service
  labels:
    app.kubernetes.io/name: build-service
    app.kubernetes.io/component: executor
    app.kubernetes.io/part-of: fuzzing-ci-cd
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: build-service
  ports:
    - name: http
      port: 8080
      targetPort: http
