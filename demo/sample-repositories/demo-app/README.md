# Demo App

Минимальный репозиторий для демонстрации executor pipeline.

Файл `mvnw` здесь намеренно является легким demo entrypoint-ом: он создает
детерминированный artifact `target/demo-app.jar` без скачивания Maven из сети.
