spring:
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP:fuzzing-service-local}
      client-id: ${KAFKA_CLIENT_ID:fuzzing-service-local}

cicd:
  executor:
    topic: ${CICD_EXECUTOR_TOPIC:jobs.fuzzing}
    worker-id: ${CICD_WORKER_ID:fuzzing-local-worker-1}

server:
  port: ${SERVER_PORT:8084}
