spring:
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: vcs-service-test
      client-id: vcs-service-test
    listener:
      auto-startup: false

management:
  health:
    kafka:
      enabled: false

logging:
  structured:
    format:
      console: ${LOGGING_STRUCTURED_FORMAT_CONSOLE:}

cicd:
  executor:
    topic: jobs.vcs
    worker-id: vcs-test-worker-1

server:
  port: 0
